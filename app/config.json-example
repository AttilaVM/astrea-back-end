{
	"debug": true,
	"verbosity": 4,
	"webserver": {
		"port": 3000,
		"root": "./front-end/dist"
	},
	"db": {
		"user": "DB_USER",
		"id": "DB_ID",
		"password": "DB_PASS",
		"opts": {
			"dialect": "DB_DIALECT",
			"host": "DB_HOST",
			"pool": {
				"max": 5,
				"min": 1,
				"acquire": 30000,
				"idle": 10000
			}
		}
	},
	"fileManager": {
		"type": "local",
		"root": "data",
		"sampleDir": "samples",
		"thumbDir": "thumbs"
	}
}
